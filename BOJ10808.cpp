#include <iostream>

using namespace std;

char input[101];

void alpha(char arr[]);

void alpha(char arr[]) {

}

int main(void) {
    ios::sync_with_stdio(0);
    cin >> input;
    alpha(input);
    return 0;
}