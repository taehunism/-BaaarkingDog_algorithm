#include <bits/stdc++.h>
using namespace std;
int box[1002][1002];
int n,m;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> n >> m; // N X M
    // data into box
    for (int i = 0; i<n; i++) 
        for (int j = 0; j<m; j++) 
            cin >> box[i][j];
    
    queue<pair<int, int>> q;
    
    for(int i = 0; i < n; i++) {
        for (int j = 0; j<m; j++) {
            
        }
    }
    
    

    return 0;
}